{
  "name": "mcp-antigravity-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "MCP Bridge between OpenClaw and Antigravity — cross-platform AI Agent task delegation",
  "author": "chaoyuqi",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev:server": "npm run dev --workspace=packages/mcp-server",
    "dev:extension": "npm run dev --workspace=packages/vscode-extension",
    "test": "npm test --workspaces",
    "clean": "rm -rf packages/*/dist packages/*/out"
  }
}
